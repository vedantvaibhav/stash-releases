import Foundation

/// Demo videos for onboarding feature screens. Both nil today — the visual
/// slot renders as a styled placeholder. Populate with bundled MP4 URLs
/// when the assets are ready; the slot renderer handles the swap.
enum OnboardingMedia {
    static let recordingDemo: URL? = nil
    static let clipboardDemo: URL? = nil
    static let filesDemo: URL? = nil
}
